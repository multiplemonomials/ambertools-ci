machine:
  services:
    - docker

dependencies:
  override:
      - docker pull ambermd/amber-build-box
      - sudo apt-get install csh xutils-dev -y

test:
  override:
    - bash devtools/ci/circleci_build.sh
